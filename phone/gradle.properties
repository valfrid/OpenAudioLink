org.gradle.jvmargs=-Xmx2g
org.gradle.parallel=true
android.useAndroidX=true
kotlin.code.style=official

# The published librespot build every APK carries. It lives on
# its own tag and its own clock -- see .github/workflows/librespot-android.yml
# and decision 18, which sets the same arrangement for the Windows build.
oal.librespot.version=0.8.0-2
